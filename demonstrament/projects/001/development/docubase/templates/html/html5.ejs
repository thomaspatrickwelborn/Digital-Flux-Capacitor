
<%
  const content=$data.content
%>
<!DOCTYPE html5>
<html lang="en">
  <head>
    <title>
      
    </title>
    <%
      for (
        const $meta of content.meta
      )
      { %>
        <meta
          <%
            for (
              const $metaAttribute of $meta.attributes
            )
            { %>
               <%= $metaAttr.key %>="<%= $metaAttr.val %>"
            <% }
          %>
        >
      <% }
    %>
    <%
      for (
        const $link of content.link
      )
      { %>
        <link
           rel="stylesheet"
           href="<%= $link.href %>"
        >
      <% }
    %>
  </head>
  <body>
  </body>
</html>