<!DOCTYPE html5>
<html lang="en"><head><title></title>
  <% for(
      const $meta➅of $data.content.meta➅)
    { %>
      <meta
        <% for(
            const $metaAttribute➅of $meta.attributes➅)
          { %>
            <%= $metaAttr.key %>="<%= $metaAttr.val %>"
            ><% }
          %><% }
    %>
  <% for(
      const $link➅of $data.content.link➅)
    { %>
      <link rel="stylesheet"
         href="<%= $link.href %>">
        <% }
    %>
  </head>
<body></body>
</html>
